'use strict';

const path = require('path');
const getLocalESLintPath = require('./utils/getLocalESLintPath');

const IgnoredPaths = require(path.join(getLocalESLintPath({ rootDir: '/Users/rogelioguzman/docker/ui/' }), 'lib', 'ignored-paths'));

const ignoredPaths = new IgnoredPaths();

console.log(ignoredPaths.ig.custom.ignoreFiles);
console.log(ignoredPaths.ig.default.ignoreFiles);
console.log(ignoredPaths.getIgnoredFoldersGlobChecker()('/'));